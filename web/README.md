# web/ — Next.js hosted UI

Not required for v0.1. The CLI and GitHub Action work without this folder.

Later: App Router, public GitHub URL paste, GitHub App for private repos, masked reports. See `notes/Next.js web app.md` and `notes/GitHub integration.md`.

Do not clone large repos inside Vercel serverless — use a worker with `git` + Python.
